import React from 'react';

import { Layout } from '@components';

const Contact: React.VFC = (): React.ReactElement => (
  <Layout title='Contact'>
    <div>dwehfwihfwirfrf</div>
  </Layout>
);

export default Contact;
